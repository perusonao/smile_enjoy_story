/// Barrel export for the whole Phase 0 domain layer (models + generators).
///
/// Pure Dart — no Flutter imports anywhere under `lib/domain/`. See the
/// Phase 0 design doc for the full set of design constraints this layer
/// must honor.
library;

export 'generation/generation.dart';
export 'models/models.dart';
