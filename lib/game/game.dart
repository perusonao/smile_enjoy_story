/// Barrel export for the whole Playable 0.1 game layer (state + engine).
///
/// Pure Dart — no Flutter imports here; the app/UI layers depend on this,
/// not the other way around.
library;

export 'engine/engine.dart';
export 'models/models.dart';
